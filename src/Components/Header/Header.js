import React,{Component} from 'react';
import { HeaderImageContainer, HeaderContent, LogoImage, Headline } from './Header.style';



export default class Header extends Component {

    render() {
        return (
            <HeaderImageContainer className="header-image-container" fluid={true}>
              
            </HeaderImageContainer>
        );
    }
}
